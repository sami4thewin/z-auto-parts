require 'spec_helper'

describe ZAutoParts do
  it 'has a version number' do
    expect(ZAutoParts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
